<div id="map"
      class="d-flex justify-content-center"
      style="width: 95%; height: 200px; border-radius: 5px;"
      data-markers="<%= @communities_coordinates.to_json %>"
      data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
</div>
